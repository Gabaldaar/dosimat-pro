rules_version = '2';
service cloud.firestore {
  match /{document=**} {
    // Default deny for any unmatched paths
    // In Prototyping Mode, we start with a secure deny-all policy.
    // Further specific rules will be added as needed.
    allow read, write: if false;
  }
}