rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regla global y robusta para un entorno administrativo: 
    // Permite cualquier operación si el usuario está logueado.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}