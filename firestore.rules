rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS ABIERTAS PARA DESARROLLO AGIL
    // La seguridad y permisos se gestionan desde la lógica de la aplicación
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}